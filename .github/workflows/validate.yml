name: Validate frontmatter

on:
  pull_request:
    branches: [main]
    paths:
      - "src/wiki/**/*.md"
      - "src/works/**/*.md"
  push:
    branches: [main]
    paths:
      - "src/wiki/**/*.md"
      - "src/works/**/*.md"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: node .github/scripts/validate-frontmatter.mjs
